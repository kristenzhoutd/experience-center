/**
 * Paid Media Campaigns Page — wrapper around the campaigns component
 * at the /pm-campaigns route (separate from personalization /campaigns)
 */
import CombinedCampaignsPage from '../components/campaigns/CombinedCampaignsPage'

export default function PmCampaignsPage() {
  return <CombinedCampaignsPage />
}
